name: Validate photo pilot

on:
  push:
  pull_request:

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install Python dependencies
        run: python -m pip install -r requirements.txt

      - name: Check JSON schemas
        run: |
          python -m json.tool schemas/foto.schema.json >/tmp/foto.schema.json
          python -m json.tool config/foto-papierabzuege.json >/tmp/foto-papierabzuege.json

      - name: Run unit tests
        run: python -m unittest discover -s tests

      - name: Validate Markdown records
        run: python scripts/validate.py
